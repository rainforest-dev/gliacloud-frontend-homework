export const POST = async () => {
  // TODO: Implement the logic to handle video upload and generate subtitles.
  // const formData = await request.formData();
  // const file = formData.get("file") as File;

  return Response.json({
    url: "/subtitles.vtt",
  });
};
